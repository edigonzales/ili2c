INTERLIS 2.3;

MODEL GeometryCHLV95_V1 (en)
  AT "https://example.com"
  VERSION "2023-01-01"
  =
  IMPORTS UNQUALIFIED INTERLIS;
  IMPORTS Units;
  IMPORTS CoordSys;

  DOMAIN MultiSurface = TEXT;
  DOMAIN MultiDirectedLine = TEXT;
  DOMAIN MultiLine = TEXT;
  DOMAIN Surface = TEXT;
  DOMAIN Coord3 = TEXT;
  DOMAIN Area = TEXT;
END GeometryCHLV95_V1.
